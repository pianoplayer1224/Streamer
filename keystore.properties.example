# Copy to keystore.properties and fill in. That file is gitignored and must never
# be committed or shared -- it grants the ability to publish updates as this app.
storeFile=../streamer-release.jks
storePassword=
keyAlias=streamer
keyPassword=
